# AI Subsystem

Processes AI requests and sets ECS-driven AI state. Registers via `AISubsystemManifest`.

For behavior/state-machine details (decision system, drivers, and shared state IDs) see `Framework/AI/Docs/AI-Overview.md`.
